.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.DS_Store
.env
# Real session content — privacy boundary, never commit any of it:
# data/2026-07-02/ is extracted from live session transcripts;
# data/2026-03-20/ is live sidecar telemetry (observations, chain, logs).
# Eval scripts take explicit --directive/--records paths to local copies.
data/

# Apollo deception-detection checkout + battery encoding caches. Download at
# runtime only: their repo has no LICENSE file, so nothing may be vendored.
eval/data/
